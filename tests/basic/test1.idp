vocabulary V {
	type A
	type B
	type C isa A
	type D contains B
	P(A)
	Q(A,B)
	F(A) : A
	partial G(A) : B
}

theory T : V {
	? x : P(x).
}

/**
 * main procedure
 */
procedure main() {
	print(T,stdoptions)
	print(stdoptions)
}

namespace N {

	procedure A() {
	}

}

