include <mx>
include <table_utils>

vocabulary V{
	P
	Q
}

theory T:V{
	P | Q.
}

structure S:V{
	
}

procedure main() {
	stdoptions.trace = true
	solution, trace = allmodels(T,S)
//	print(tabletostring(trace))
	if (#trace==8 and 
		trace[1]["type"]=="assign" and 
		trace[2]["type"]=="assign" and 
		trace[3]["type"]=="assign" and 
		trace[4]["type"]=="backtrack" and 
		trace[5]["type"]=="assign" and 
		trace[6]["type"]=="assign" and 
		trace[7]["type"]=="backtrack" and 
		trace[8]["type"]=="assign" and 
		trace[1]["dl"]==0 and
		trace[2]["dl"]==1 and
		trace[3]["dl"]==1 and 
		trace[4]["dl"]==0 and 
		trace[5]["dl"]==0 and 
		trace[6]["dl"]==1 and 
		trace[7]["dl"]==0 and 
		trace[8]["dl"]==0) then
			return 1
	else return 0
	end
}
