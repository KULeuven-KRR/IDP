vocabulary V{
	TakeOff(int)
	Flying(int)
	Landed(int)
	Done(int)
	Start(int)
}
vocabulary Vout{
	
}
theory T:V{
	?t: Done(t).
	
	{
		TakeOff(t) <- Start(t).
		Flying(t) <- TakeOff(t-1).
		Flying(t) <- Flying(t-1).
		Landed(t) <- Flying(t-10).
		Done(t) <- Landed(t-1).
	}
}
structure S:V{
}