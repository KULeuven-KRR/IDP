vocabulary V{
	type partition
	type number isa int
	in(partition, number)
}

vocabulary Vout{
	
}

theory T: V{
	!n: ?p: in(p, n).
	!p n: in(p, n) => !p2: in(p2, n) => p=p2.
}

structure S: V {
	partition = {1..1000}
	number = {1..40}
}