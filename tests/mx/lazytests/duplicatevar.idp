vocabulary V{
	type X isa int	
	XPos(X, X)
}
theory T:V{
	?x: XPos(x, x).
	XPos(1,3).
}
structure S:V{
	X = {1..20}
}
procedure main(){
	stdoptions.groundverbosity=10
	stdoptions.satverbosity=0
	stdoptions.groundlazily=true
	print(modelexpandpartial(T, S))
}