vocabulary V{
	type x isa nat
	p(x)
	q(x,x)
	r(x)
	a
}

vocabulary Vout{
	a
}

theory T:V{
	!x: p(x) => !y: r(y) & a & !z: q(x,z).
	?x: p(x).
	?x: r(x).
}

structure S:V{
	x is nat
}

procedure getExpectedNbOfModels(){
	return 2
}
