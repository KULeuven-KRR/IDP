vocabulary V{
	p
	q
	r
}

theory T:V{
	~(p | q | r).
}

structure S:V{
}

#include <mx>

procedure main(){
	local expected = 1
	local sols = mx::all(T, S)
	if(#(sols)==expected) then
		print("OK")
	else
		print("ERROR, should have been "..expected..", but found "..#(sols).." solutions.")
	end
}
