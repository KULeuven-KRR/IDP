vocabulary V{
	type x
	p(x)
}

theory T:V{
	?x: p(x).
}

structure S:V{
	x = {1..3}
}

#include <mx>

procedure main(){
	local sols = mx::all(T, S)
	if(#(sols)==7) then
		print("OK")
	else
		print("ERROR, should have been 7, but found "..#(sols).." solutions.")
	end
}
