vocabulary V{
	type x
	p(x)
	q(x)
}

theory T:V{
	!x: p(x) | q(x).
}

structure S:V{
	x = {1..3}
}

#include <mx>

procedure main(){
	local sols = mx::all(T, S)
	if(#(sols)==27) then
		print("OK")
	else
		print("ERROR")
	end
}
