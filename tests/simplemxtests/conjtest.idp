vocabulary V{
	p
	q
	r
}

theory T:V{
	p & q & r.
}

structure S:V{
}

#include <mx>

procedure main(){
	local sols = mx::all(T, S)
	if(#(sols)==1) then
		print("OK")
	else
		print("ERROR, should have been 7, but found "..#(sols).." solutions.")
	end
}
