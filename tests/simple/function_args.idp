vocabulary V {
	type T
	type S
	F(T, T) : S
}
structure S : V {
	T = {1;2}
	S = {2;3;4}
	F = {1,1 -> 2; 1,2 -> 3; 2,1 -> 3; 2,2 -> 4}
}
procedure main() {
		if pcall(S[V::F], 1) then
			return 0
		elseif pcall(S[V::F], 1, 1, 2) then
			return 0
		elseif S[V::F](1,1) ~= 2 then
			return 0
		else
			return 1
		end
}
